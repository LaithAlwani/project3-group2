import React from "react";

const LoginRequired = () => {
	return (
		<>
			<p>Sorry, you must be logged in to view that content</p>
			<button>Login</button>
		</>
	);
};

export default LoginRequired;
